spring:
  application:
    name: greeting-service

greeting-service:
  greeting: "Olá"
  default-value: "Profile"

# allow show all endpoints in Actuator, see /actuator
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"